<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.TestDao">

    <insert id="insertData" parameterType="TestDto">
        INSERT INTO EXEMTEST.TEST (grade, level, station, fineDust, ultrafineDust, date)
        VALUES (#{grade}, #{level}, #{station}, #{fineDust},#{ultrafineDust}, #{date})
    </insert>

</mapper>